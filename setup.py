#!/usr/bin/python

from setuptools import setup

setup(
		name = 'PyLogger',
		version = '1.0',
		description = 'A context based logging utility for Python.',
		author = 'Aaron Karper',
		author_email = 'maergil@gmail.com',
		packages = ['PyLogger'],
		install_requires = [
			'setuptools'])
